 public static void exception(Enum a){
        String text=a.name();
        text=text.replaceAll("_", " ");

       text=text.substring(0,1).toUpperCase()+text.substring(1).toLowerCase();
        System.out.println(text);
    }